const initialState = {
  schedule: [],
  fetching: false,
  fetched: false,
  error: null
}

function scheduleReducer(state=initialState, action) {
      switch (action.type) {
          default:
          return state

      }
  }

  export default scheduleReducer;